package internal

// Session type constants
// extracted to a constant to ensure easier reproducability
// easier maintainability as if this changes, it will be able to rewrite through the application
const (
	SessionFocus = "FOCUS"
	SessionBreak = "BREAK"
)
